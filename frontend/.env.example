# Optional: Backend base URL for loading uploads (e.g. complaint images).
# Leave unset in development when using Vite proxy; set in production to your API origin.
# Example: https://api.yourdomain.com
# VITE_API_BASE_URL=
